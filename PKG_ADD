PACKAGE='pdsit';
VERSION='1.0.0';

path_pfx=[pkg('prefix'),filesep,PACKAGE,'-',VERSION];

parts={'basics','ceo','omega'};

for k=1:length(parts)
	addpath([path_pfx,filesep,'pdsit',filesep,parts{k}]);
end
